import { DisplayChildDirective } from './display-child.directive';

describe('DisplayChildDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayChildDirective();
    expect(directive).toBeTruthy();
  });
});
